# Only needed to REBUILD the database from the source workbooks.
# Reading dyingcyrus.db needs nothing at all: sqlite3 is in the Python stdlib,
# and dyingcyrus.py has no dependencies.
openpyxl>=3.1.0

# Optional, for analysis:
# pandas>=2.0.0
